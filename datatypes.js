//datypes in JS
//primitive -> number, String, null, undefined, boolean
    // new types --> BigInt, Symbol

//non-primitive ->object, functions and arrays
    //new types ->map, set, weakset and weakmap
var a=10;
var str=" I am a string" + a +" an variable is used";
console.log(str);

// var s="fdffd
// sfdf"

var s=`fsfsfs s
d sdgg 
fssgsg`;
console.log(s);

